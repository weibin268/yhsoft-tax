<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE configuration  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 引入外部配置文件 -->
	<!-- <properties resource="config/sqlserver.properties"></properties> -->
	<properties resource="config/mysql.properties"></properties>


<!--
    <settings>
    	<setting name="logImpl" value="LOG4J"/>
    </settings>
-->

<!-- 	
	<typeAliases>
		<package name="com.zhuang.autocode.model"/>
	</typeAliases>
 -->
 
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<mapper resource="com/zhuang/fileupload/mapper/SysFileUpload.xml" />
	</mappers>

</configuration>